<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="BlackMirror.Theme" parent="Theme.Design.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textViewStyle">@style/BlackMirror.Theme.TextView</item>
    </style>

    <style name="BlackMirror.Theme.TextView" parent="android:Widget.TextView">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="BlackMirror.Theme.CalendarView" parent="android:Widget.CalendarView">
        <item name="android:focusedMonthDateColor">@android:color/white</item>
        <item name="android:weekNumberColor">@android:color/white</item>
        <!--<item name="android:weekDayTextAppearance">@style/TextAppearance.Medium</item>-->
        <item name="android:dateTextAppearance">@style/BlackMirror.Theme.CalendarView.DateText
        </item>
    </style>

    <style name="BlackMirror.Theme.CalendarView.WeekDay" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="BlackMirror.Theme.CalendarView.DateText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/shape_frame</item>
        <item name="android:colorBackground">@android:color/holo_blue_dark</item>
        <item name="android:src">@android:color/darker_gray</item>
    </style>

</resources>
